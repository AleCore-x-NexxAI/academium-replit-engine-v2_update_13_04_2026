You're right — students should never see a blank white screen. Let me add comprehensive error handling across the entire auth flow.

Now I have the full picture. Here's everything that needs fixing:

Router() returns null during loading — blank white screen
No error boundary — any React crash = white screen
RoleSelection does a raw window.location.href — no loading state, no timeout, no error handling
No auth-failure detection — if callback fails, user is just stuck
useAuth doesn't expose errors — can't show anything useful
Let me also check the queryClient default error handling:

Now I have the full picture. The default queryFn throws on 401, and there's no error boundary, no loading indicator, no timeout detection — multiple failure paths that all lead to white screens. Let me fix everything comprehensively.

Now let me add translation keys for auth errors, create an error boundary, and fix the loading/timeout states:

Now I have everything I need. Let me add all the translation keys and create the error boundary + fix the auth flow:

Now let me create the ErrorBoundary component and update the App.tsx and RoleSelection:

Now let me update App.tsx with the loading screen, error boundary, and auth-failure detection:

Now let me update RoleSelection to show a loading/timeout state when the user clicks sign in, and detect auth callback failures:

Now let me rewrite RoleSelection with proper loading/error states:

Now let me also add server-side error handling for the auth callback — if the OIDC callback fails, redirect to RoleSelection with an error flag instead of leaving the user stranded:

Now fix the server-side callback error handling — instead of redirecting to /api/login (which creates an infinite loop), redirect to /select-role?auth_error=true:

Now let me also add a try-catch around the upsert user logic in the callback to prevent server crashes from breaking the flow:

Now let me restart the app and check the logs:

App is running clean. Now let me run the code review:

Good catches from the review. Let me fix the issues:

Issue 1: Admin verification is already handled correctly — I checked the original code: handleCodeSubmit calls /api/auth/verify-admin-code which stores verification in a session, then window.location.href = /api/fresh-login?role=admin&verified=true. Let me verify the fresh-login flow actually preserves the admin verification via the verified=true param.

Let me check the admin verification flow more carefully — the review found that admin login might silently downgrade to student:

The architect found a real issue: the admin verification gets lost when /api/fresh-login destroys the session. Let me fix that too, and also fix the timeout retry edge case:

Now fix the admin verification in /api/fresh-login — preserve it via a signed cookie BEFORE destroying the session:

Now fix the timeout retry edge case in App.tsx — ensure the timeout timer restarts on retry:

Now let me also add encodeURIComponent for the role param in the error redirects:

Now also fix the encodeURIComponent in the DB error catch block:

Everything is running clean — no errors. Now let me write the commit message and wrap up:

Here's a summary of everything I fixed:

The core problem: When a student clicked "Sign In," multiple failure paths could leave them staring at a completely blank white screen with no way to recover.

What I fixed (6 layers of protection):

Branded loading screen — Instead of a blank white page during auth check, students now see the Academium logo with a "Loading..." message.

Timeout detection — If the auth check takes more than 8 seconds, a clear error screen appears with a retry button and a "Go back to home" option.

React Error Boundary — If any unexpected crash happens anywhere in the app, instead of a white screen, users see a friendly error card with a "Refresh page" button and technical details they can expand if needed.

Login redirect state — When the student clicks a role card, they now see a "Redirecting to sign in..." screen with a spinner and a manual fallback link. If nothing happens within 15 seconds, it automatically shows a failure screen.

Server-side callback protection — If the Replit authentication callback fails (network issue, cookie problem, database error), instead of getting stuck in a redirect loop, the server now sends users back to the role selection page with a clear error message, troubleshooting tips (try Chrome, disable ad blockers, use incognito), and a retry button.

Admin verification fix — Also fixed a pre-existing bug where admin verification was lost during the login flow because the session was destroyed before the verification could be carried forward.

All error messages are fully translated in Spanish and English.